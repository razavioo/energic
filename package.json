{
    "name": "energic-iot-monorepo",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "broker",
        "sensor",
        "admin",
        "visualizer"
    ],
    "scripts": {
        "dev:broker": "npm run dev --workspace=broker",
        "dev:sensor": "npm run dev --workspace=sensor",
        "dev:admin": "npm run dev --workspace=admin",
        "dev:visualizer": "npm run dev --workspace=visualizer",
        "build": "npm run build --workspace=visualizer"
    }
}